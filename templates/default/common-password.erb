# This line may need to be changed, I have seen md5, etc so you may want to change this to what was existing prior
password   sufficient   pam_unix.so nullok obscure sha512
password   sufficient   pam_mysql.so config_file=<%= @config_file %>
password        requisite                       pam_deny.so
password        required                        pam_permit.so
