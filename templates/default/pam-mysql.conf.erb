# Generated by CHEF -- Do not edit

users.host              = <%= @db_host %>
users.database          = <%= @database %>
users.db_user           = <%= @db_user %>
users.db_passwd         = <%= @db_passwd %>
#users.where_clause     = (host)
users.table             = <%= @users_table %>
#users.update_table     = (update_table)
users.user_column       = <%= @users_table -%>.user_name
users.password_column   = <%= @users_table -%>.password
users.status_column     = status
users.password_crypt    = <%= @encryption_type %> 
<% if @encryption_type == 3 %>
users.use_md5          = yes
<% end %>
#users.use_323_password = (use_323_passwd)
#users.where_clause     = (where)
#users.disconnect_every_operation       = (disconnect_every_op) *1

# Verbose dumps debug info to auth.log from pam_mysql, useful for troubleshooting - Disable in production as sensitive info may be dumped to log files
# un comment to get verbose logging in auth.log for debugging
<% if @verbose %>
verbose                 = (verbose)
<% else %>
#verbose                 = (verbose)
<% end %>

<% if @sql_logging %>
log.enabled             = (sqllog)
log.table               = <%= @debug_log_table %>
log.message_column      = msg
log.pid_column          = pid
log.user_column         = user
log.host_column         = host
log.rhost_column        = rhost
log.time_column         = time
<% end %>
